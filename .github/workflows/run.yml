name: Windows Patch Deployment Demo

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  run-demo:
    runs-on: windows-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Show Trigger Info
        shell: powershell
        run: ./install-patch-automatically.ps1
          
